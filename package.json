{
  "name": "supergrill-agents",
  "private": true,
  "type": "module",
  "version": "1.1.0",
  "scripts": {
    "test": "bun test",
    "check:sh": "sh -n install.sh",
    "check:ps": "bun tests/check-powershell-syntax.ts",
    "check": "bun run check:sh && bun run check:ps && bun test"
  }
}
